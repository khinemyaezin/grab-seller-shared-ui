# Changesets

Run `npm run changeset` after changing any published package.

These packages are configured as a fixed release group:

- `@khinemyaezin/seller-contracts`
- `@khinemyaezin/seller-api`
- `@khinemyaezin/seller-ui`

The release workflow updates and publishes them together.
